// Gatsby SSR code

// You can also wrap with TSX components here too, like so
// import { WrapPageElement, WrapRootElement } from '../src/components';
// export const wrapRootElement = WrapRootElement;